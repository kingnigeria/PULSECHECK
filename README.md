# PULSECHECK
PulseCheck is a lightweight socket-based remote task manager built from the project documentation in this folder. It uses a Manager/Worker architecture with:  TCP handshake authentication TCP task and telemetry channel UDP alert channel Fernet encryption psutil system metrics Heartbeat-based timeout detection
